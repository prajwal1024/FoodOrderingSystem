🐧 Run on Linux:
Transfer the food-ordering folder to Linux (via Git)
Then:
bash
cd ~/food-ordering
python3 -m venv venv
source venv/bin/activate
pip install flask
python3 app.py
Open http://127.0.0.1:5000 in your browser.

Windows
cd C:\Users\prajw\food-ordering
pip install flask
python app.py
